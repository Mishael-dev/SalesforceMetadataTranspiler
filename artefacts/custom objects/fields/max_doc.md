# auto-number

# Field Documentation: Auto Number

This document breaks down the Salesforce **AutoNumber Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `FieldName__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the auto-number sequence.

```xml
<fullName>AutoNumber__c</fullName>
<label>AutoNumber</label>
<displayFormat>ASSET-{YYYY}-{MM}-{00000}</displayFormat>
<description>Unique identifier for global company assets, tracking the year and month of creation.</description>
<inlineHelpText>This ID is generated automatically. Format: ASSET-YEAR-MONTH-NUMBER</inlineHelpText>
<startingNumber>1</startingNumber>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<externalId>false</externalId>
<trackHistory>false</trackHistory>
```
---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>AutoNumber</type>
<trackTrending>false</trackTrending>
```

---
## Json Shape

This section represents the core field definition as a JSON object.

```json
 {
    type: "AutoNumber",
    label: "Asset ID",
    fullName: "Asset_ID__c",
    displayFormat: "ASSET-{YYYY}-{0000}",
    description: "Automatically generated identifier for each asset record.",
    inlineHelpText: "Example: ASSET-2026-0001",
    startingNumber: 1
  }
```
---

## 5. Validation Rules

This section documents the validation rules applied to **AutoNumber** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the AutoNumber field schema.

* `type` must be exactly `AutoNumber`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `startingNumber`, if provided, must be a number.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* `displayFormat` is **required** for AutoNumber fields.
* `startingNumber` must be greater than or equal to `0`.
* `externalId` must be `false` (AutoNumber fields cannot be External IDs).

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **40 characters**.
* `displayFormat` must not exceed **255 characters**.
* `displayFormat` must contain **at least one numeric placeholder** (`{0}`, `{00}`, `{000}`, etc.).
* Date tokens in `displayFormat` are restricted to supported Salesforce placeholders (`{YYYY}`, `{YY}`, `{MM}`, `{DD}`).
* AutoNumber fields **cannot be encrypted** or marked as unique.
* AutoNumber fields cannot be used as **lookup filters** or **external identifiers**.

Violations at this level should fail fast with actionable error messages before packaging.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* If a field exists:

  * Prevent changes to `type` (AutoNumber → other types is illegal).
  * Prevent changes to `displayFormat` on existing AutoNumber fields.
  * Prevent changes to `startingNumber` if records already exist.
* Validate that the target object is **not managed-package locked**.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value      |
| ------------------ | -------------- | ------ | ------------------ |
| `<fullName>`       | fullName       | LLM    | Derived from Label |
| `<label>`          | label          | LLM    | N/A                |
| `<displayFormat>`  | displayFormat  | LLM    | `{0000}`           |
| `<startingNumber>` | startingNumber | LLM    | 1                  |
| `<description>`    | description    | LLM    | Empty String       |
| `<inlineHelpText>` | inlineHelpText       | LLM    | Empty String       |
| `<externalId>`     | externalId     | UI     | false              |
| `<trackHistory>`   | trackHistory   | UI     | false              |
| `<type>`           | type           | System | AutoNumber         |
| `<trackTrending>`  | N/A            | System | false              |
---

# checkbox

# Field Documentation: Checkbox

This document breaks down the Salesforce **Checkbox Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `checkbox__c.field-meta.xml`.

## 1. The Outer Container

This is the Salesforce Metadata API wrapper that defines a single custom field. It provides the XML namespace and ensures the field is correctly interpreted during deployment.

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These elements define the **business intent and semantic meaning** of the Checkbox field. They must be explicitly generated by the LLM because they directly affect how the field behaves and is understood by users.

```xml
<fullName>checkbox__c</fullName>
<label>checkbox</label>
<description>checkbox description</description>
<inlineHelpText>checkbox help text</inlineHelpText>
<defaultValue>false</defaultValue>
```

### Element Responsibilities

* **`fullName`**

  * Defines the API name of the field.
  * Must be unique within the object and end with `__c`.

* **`label`**

  * Human-readable name displayed in the Salesforce UI.

* **`description`**

  * Optional documentation for admins and developers.

* **`inlineHelpText`**

  * Contextual help text displayed to end users.

* **`defaultValue`**

  * Defines the default boolean state when a new record is created.
  * For Checkbox fields, valid values are `true` or `false`.

---

## 3. Supporting Features (React Flow UI)

These elements control optional platform behaviors. They are surfaced in the UI as toggles and are not part of the core field intent.

```xml
<trackHistory>false</trackHistory>
```

### Element Responsibilities

* **`trackHistory`**

  * Enables or disables field history tracking.
  * Applicable to Checkbox fields.

---

## 4. Others (Transpiler Injected)

These elements are required by Salesforce or are injected automatically by the transpiler to ensure a valid metadata definition.

```xml
<type>Checkbox</type>
<trackTrending>false</trackTrending>
```

### Element Responsibilities

* **`type`**

  * Declares the Salesforce field type.
  * Must be exactly `Checkbox`.

* **`trackTrending`**

  * Controls whether the field is tracked for trend analytics.
  * Not user-configurable in most orgs and defaults to `false`.

---
## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "Checkbox",
  "label": "checkbox",
  "fullName": "checkbox__c",
  "description": "checkbox description",
  "inlineHelpText": "checkbox help text",
  "defaultValue": false
}
```
---
## 5. Validation Rules

This section documents the validation rules applied to **Checkbox** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON structure is valid and complete.

* `type` must be exactly `Checkbox`.
* `fullName` must be a non-empty string ending with `__c`.
* `label` must be a non-empty string.
* `defaultValue` must be a boolean (`true` or `false`).
* `trackHistory` and `trackTrending` must be boolean values if present.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and correct field behavior.

* `defaultValue` must be either `true` or `false` (no expressions or formulas allowed).
* Checkbox fields **cannot** have precision, scale, length, or formula-related attributes.
* `trackHistory` may only be enabled if Field History Tracking is enabled on the object.

---

### Level 3: Platform Constraints (The Linter)

Enforces Salesforce hard limits and prohibitions that cannot be reliably enforced via schema validation alone.

* `fullName` (excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **40 characters**.
* `inlineHelpText` must not exceed **255 characters**.
* Checkbox fields **cannot** be marked as `externalId` or `unique`.
* Checkbox fields cannot be encrypted.
* Checkbox fields cannot be used as roll-up summary targets.

Violations at this level should fail fast with clear, actionable error messages.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

Validations that require awareness of the target Salesforce org.

* Verify no existing field with the same `fullName` exists on the target object.
* If the field already exists:

  * Prevent changes to `type`.
  * Prevent conversion from Checkbox to another field type.
* Validate that the target object is not locked by a managed package.
* Ensure no namespace collisions exist in namespaced orgs.

Failures at this level must be surfaced as **deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value |
| ------------------ | -------------- | ------ | ------------- |
| `<fullName>`       | fullName       | LLM    | Derived       |
| `<label>`          | label          | LLM    | N/A           |
| `<description>`    | description    | LLM    | Empty String  |
| `<inlineHelpText>` | inlineHelpText | LLM    | Empty String  |
| `<defaultValue>`   | defaultValue   | LLM    | false         |
| `<trackHistory>`   | trackHistory   | UI     | false         |
| `<type>`           | type           | System | Checkbox      |
| `<trackTrending>`  | trackTrending  | System | false         |

---

# currency

# Field Documentation: Currency

This document breaks down the Salesforce **Currency Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `currency__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the currency field.

```xml
<fullName>currency__c</fullName>
<label>currency</label>
<defaultValue>12</defaultValue>
<description>currency description</description>
<inlineHelpText>currency help</inlineHelpText>
<precision>14</precision>
<scale>2</scale>
<required>true</required>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<trackHistory>false</trackHistory>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>Currency</type>
<trackTrending>false</trackTrending>
```

---

## Json Shape

```json
{
  "type": "Currency",
  "label": "currency",
  "fullName": "currency__c",
  "defaultValue": 12,
  "description": "currency description",
  "inlineHelpText": "currency help",
  "precision": 14,
  "scale": 2,
  "required": true
}
```
---

## 5. Validation Rules

This section documents the validation rules applied to **Currency** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the Currency field schema.

* `type` must be exactly `Currency`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `defaultValue`, if provided, must be a number.
* `precision` must be a positive integer.
* `scale` must be a non-negative integer less than or equal to `precision`.
* `required` must be a boolean.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* `scale` cannot exceed `precision`.
* `defaultValue` must fit within the specified `precision` and `scale`.
* `required` being `true` means `defaultValue` must be provided.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **80 characters**.
* `precision` maximum is **18**.
* `scale` maximum is **8**.
* Currency fields cannot be encrypted.
* Default value must be compatible with org currency settings.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* Prevent changing `precision` or `scale` on existing fields if records exist.
* Ensure that the target object is not managed-package locked.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value      |
| ------------------ | -------------- | ------ | ------------------ |
| `<fullName>`       | fullName       | LLM    | Derived from Label |
| `<label>`          | label          | LLM    | N/A                |
| `<defaultValue>`   | defaultValue   | LLM    | 0                  |
| `<description>`    | description    | LLM    | Empty String       |
| `<inlineHelpText>` | inlineHelpText | LLM    | Empty String       |
| `<precision>`      | precision      | LLM    | 18                 |
| `<scale>`          | scale          | LLM    | 2                  |
| `<required>`       | required       | LLM    | false              |
| `<trackHistory>`   | trackHistory   | UI     | false              |
| `<type>`           | type           | System | Currency           |
| `<trackTrending>`  | N/A            | System | false              |

---


---

# date

# Field Documentation: Date

This document breaks down the Salesforce **Date Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `date__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM defines the business logic, identity, and default value of the Date field.

```xml
<fullName>date__c</fullName>
<label>date</label>
<defaultValue>DATE(1,1,1)</defaultValue>
<description>date description</description>
<inlineHelpText>date help</inlineHelpText>
<required>true</required>
```

---

## 3. Supporting Features (React Flow UI)

These fields are displayed in the React Flow interface for toggling platform features but are not core to the field's identity.

```xml
<trackHistory>false</trackHistory>
```

---

## 4. Others (Transpiler Injected)

These fields are automatically added by the transpiler or required by Salesforce to ensure the XML is valid.

```xml
<type>Date</type>
<trackTrending>false</trackTrending>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "Date",
  "label": "date",
  "fullName": "date__c",
  "defaultValue": "DATE(1,1,1)",
  "description": "date description",
  "inlineHelpText": "date help",
  "required": true
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **Date** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

* `type` must be exactly `Date`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `defaultValue` must be a valid Salesforce DATE expression or empty.
* `required` must be a boolean.

---

### Level 2: Logical Validation (The Context)

* `defaultValue` should represent a valid date (YYYY,MM,DD). Values like `DATE(1,1,1)` are syntactically correct but may require context validation.
* `required` cannot be false if business logic mandates the field to always have a value.

---

### Level 3: Platform Constraints (The Linter)

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **40 characters**.
* Date fields **cannot** have unsupported functions or formulas in `defaultValue`.
* `defaultValue` expressions must be compatible with Salesforce DATE formula syntax.
* `required` fields must have a valid default or be populated during record creation.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

* Verify that no existing field with the same `fullName` already exists on the target object.
* Prevent changes to `type` once the field is created.
* Confirm that changing `required` does not conflict with existing records that may have null values.
* Validate that the target object is **not managed-package locked**.
* Detect namespace collisions in namespaced orgs.
---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value      |
| ------------------ | -------------- | ------ | ------------------ |
| `<fullName>`       | fullName       | LLM    | Derived from Label |
| `<label>`          | label          | LLM    | N/A                |
| `<defaultValue>`   | defaultValue   | LLM    | None               |
| `<description>`    | description    | LLM    | Empty String       |
| `<inlineHelpText>` | inlineHelpText | LLM    | Empty String       |
| `<required>`       | required       | LLM    | false              |
| `<trackHistory>`   | trackHistory   | UI     | false              |
| `<type>`           | type           | System | Date               |
| `<trackTrending>`  | N/A            | System | false              |

---

# date_time

# Field Documentation: DateTime

This document breaks down the Salesforce **DateTime Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `date_time__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the DateTime field.

```xml
<fullName>date_time__c</fullName>
<label>date_time</label>
<description>date time description</description>
<inlineHelpText>date time help</inlineHelpText>
<defaultValue>NOW()</defaultValue>
<required>false</required>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<trackHistory>false</trackHistory>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>DateTime</type>
<trackTrending>false</trackTrending>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "DateTime",
  "label": "date_time",
  "fullName": "date_time__c",
  "defaultValue": "NOW()",
  "description": "date time description",
  "inlineHelpText": "date time help",
  "required": false
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **DateTime** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the DateTime field schema.

* `type` must be exactly `DateTime`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `defaultValue` must be a valid Salesforce DateTime expression (e.g., `NOW()`).
* `required` must be a boolean.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* `defaultValue` should be compatible with the `DateTime` type.
* `required` false allows `null` values; if true, `defaultValue` must be set or handled.
* Cross-field logical consistency (e.g., `trackHistory` does not conflict with default values).

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **40 characters**.
* `defaultValue` for DateTime fields cannot exceed **255 characters**.
* `DateTime` fields cannot have unsupported formulas.
* `DateTime` fields cannot be encrypted.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* If a field exists:

  * Prevent changes to `type` (DateTime → other types is illegal).
  * Prevent incompatible changes to `defaultValue` if data exists.
  * Validate that `required` status does not conflict with existing data.
* Validate that the target object is **not managed-package locked**.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value      |
| ------------------ | -------------- | ------ | ------------------ |
| `<fullName>`       | fullName       | LLM    | Derived from Label |
| `<label>`          | label          | LLM    | N/A                |
| `<description>`    | description    | LLM    | Empty String       |
| `<inlineHelpText>` | inlineHelpText | LLM    | Empty String       |
| `<defaultValue>`   | defaultValue   | LLM    | None               |
| `<required>`       | required       | LLM    | false              |
| `<trackHistory>`   | trackHistory   | UI     | false              |
| `<type>`           | type           | System | DateTime           |
| `<trackTrending>`  | N/A            | System | false              |
---

# email

# Field Documentation: Email

This document breaks down the Salesforce **Email Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `email__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the Email field.

```xml
<fullName>email__c</fullName>
<label>email</label>
<description>email description</description>
<inlineHelpText>email help</inlineHelpText>
<required>true</required>
<caseSensitive>true</caseSensitive>
<unique>true</unique>
<externalId>false</externalId>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<trackHistory>false</trackHistory>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>Email</type>
<trackTrending>false</trackTrending>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "Email",
  "label": "email",
  "fullName": "email__c",
  "description": "email description",
  "inlineHelpText": "email help",
  "required": true,
  "caseSensitive": true,
  "unique": true,
  "externalId": false
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **Email** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the Email field schema.

* `type` must be exactly `Email`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `required`, `caseSensitive`, `unique`, and `externalId` must be booleans.
* `description` and `inlineHelpText` must be strings.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* `required` true requires that all records have a valid email.
* `unique` true enforces uniqueness across the object.
* `externalId` cannot be true if `unique` is false.
* `caseSensitive` affects uniqueness enforcement.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **80 characters**.
* Email field values must comply with Salesforce email regex patterns.
* `unique` fields must not violate platform uniqueness constraints.
* Email fields cannot be encrypted if marked as unique.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* If a field exists:

  * Prevent changes to `type` (Email → other types is illegal).
  * Prevent removal of `unique` if data already exists.
  * Ensure `required` changes do not conflict with existing null values.
* Validate that the target object is **not managed-package locked**.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value      |
| ------------------ | -------------- | ------ | ------------------ |
| `<fullName>`       | fullName       | LLM    | Derived from Label |
| `<label>`          | label          | LLM    | N/A                |
| `<description>`    | description    | LLM    | Empty String       |
| `<inlineHelpText>` | inlineHelpText | LLM    | Empty String       |
| `<required>`       | required       | LLM    | false              |
| `<caseSensitive>`  | caseSensitive  | LLM    | false              |
| `<unique>`         | unique         | LLM    | false              |
| `<externalId>`     | externalId     | LLM    | false              |
| `<trackHistory>`   | trackHistory   | UI     | false              |
| `<type>`           | type           | System | Email              |
| `<trackTrending>`  | N/A            | System | false              |
---

# formula

# Field Documentation: Formula Field

This document breaks down the Salesforce **Formula Custom Field XML** based on the exported metadata. It separates the core logic from the supporting UI features and system-injected metadata.

---

## 1. The Outer Container

Stored in `Formula_Field__c.field-meta.xml`.

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must define the formula logic, the return type, and the identity of the field. Note that characters like `"` and `&` are XML-encoded as `&quot;` and `&amp;`.

```xml
<fullName>Formula_Field__c</fullName>
<label>Formula Field</label>
<type>Text</type>
<formula>IF(NOT(ISBLANK(AutoNumber__c)), &quot;ACTIVE: &quot; &amp; Name, &quot;INACTIVE&quot;)</formula>
<formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
<description>A kitchen sink formula field that combines a checkbox status with the record name.</description>
<inlineHelpText>Help Text: Shows the status prefix followed by the name.</inlineHelpText>
```

---

## 3. Supporting Features (React Flow UI)

These fields are defaulted by the system/transpiler but are visible in the React Flow interface for user modification.

```xml
<externalId>false</externalId>
<required>false</required>
<unique>false</unique>
<trackHistory>false</trackHistory>
```

---

## 4. Others (Transpiler Injected)

These fields are automatically added by the transpiler to ensure compatibility with the Salesforce Metadata API.

```xml
<trackTrending>false</trackTrending>
```

---
## Json Shape

This section represents the core field definition as a JSON object.

```json
 {
    type: "Formula",
    label: "Asset Status Label",
    fullName: "Asset_Status_Label__c",
    returnType: "Text",
    formula: 'IF(NOT(ISBLANK(Asset_ID__c)), "ACTIVE: " & Name, "INACTIVE")',
    blankOption: "BlankAsZero",
    description: "Human-readable status based on assigned ID.",
    inlineHelpText: "Shows ACTIVE or INACTIVE followed by the record name."
 }
```
---

## 5. Validation Rules

This section documents the validation rules applied to **Formula** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the Formula field schema.

* `formula` must be a non-empty string.
* `returnType` must be a valid Salesforce formula return type (`Text`, `Number`, `Currency`, `Percent`, `Date`, `DateTime`, `Checkbox`).
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `formulaTreatBlanksAs` must be a valid enum value.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and dependency correctness.

* `formula` must reference only fields that exist on the same object.
* Referenced fields must be compatible with the selected `returnType`.
* `formulaTreatBlanksAs` must be compatible with the `returnType` (e.g., `BlankAsZero` is invalid for `Text`).
* `externalId` must be `false` (Formula fields cannot be External IDs).
* `required` must be `false` (Formula fields are implicitly calculated).
* `unique` must be `false` (Formula fields cannot enforce uniqueness).

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are impractical to encode fully in Zod schemas.

* Formula length must not exceed **3,900 characters**.
* Compiled formula size must not exceed Salesforce limits.
* `label` must not exceed **40 characters**.
* `fullName` (API name excluding `__c`) must not exceed **40 characters**.
* Formula fields **cannot reference themselves**, directly or indirectly.
* Formula fields cannot reference:

  * Encrypted fields
  * Long Text Area fields
  * Rich Text Area fields
* Cross-object references are limited by Salesforce relationship depth constraints.
* Formula fields cannot be used as **External IDs**, **Unique fields**, or **Primary Keys**.

Violations at this level should be surfaced as **linter errors** with precise feedback.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* If the Formula field already exists:

  * Prevent changes to `returnType`.
  * Prevent incompatible changes to `formulaTreatBlanksAs`.
  * Validate that the updated formula does not exceed compiled size limits.
* Validate that all referenced fields still exist in the target org.
* Detect dependency chains that would break existing formulas or roll-up summaries.
* Validate that the target object is not **managed-package locked**.

Failures at this level should be reported as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag                  | JSON Key    | Source     | Default Value      |
| ------------------------ | ----------- | ---------- | ------------------ |
| `<fullName>`             | fullName    | LLM        | Derived from Label |
| `<label>`                | label       | LLM        | N/A                |
| `<formula>`              | formula     | LLM        | N/A                |
| `<type>`                 | returnType  | LLM        | Text               |
| `<formulaTreatBlanksAs>` | blankOption | LLM/System | BlankAsZero        |
| `<description>`          | description | LLM        | Empty String       |
| `<inlineHelpText>`       | inlineHelpText    | LLM        | Empty String       |
| `<externalId>`           | externalId  | UI         | false              |
| `<required>`             | required    | UI         | false              |
| `<unique>`               | unique      | UI         | false              |
| `<trackTrending>`        | N/A         | System     | false              |
---

# geolocation

# Field Documentation: Geo Location

This document breaks down the Salesforce **Location (Geolocation) Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `geolocation__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the Location field.

```xml
<fullName>geolocation__c</fullName>
<label>geolocation</label>
<description>geolocation description</description>
<inlineHelpText>geolocation help</inlineHelpText>
<required>false</required>
<displayLocationInDecimal>false</displayLocationInDecimal>
<scale>2</scale>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<trackHistory>false</trackHistory>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>Location</type>
<trackTrending>false</trackTrending>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "Location",
  "label": "geolocation",
  "fullName": "geolocation__c",
  "description": "geolocation description",
  "inlineHelpText": "geolocation help",
  "required": false,
  "displayLocationInDecimal": false,
  "scale": 2
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **Location (Geolocation)** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the Location field schema.

* `type` must be exactly `Location`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `required` must be a boolean.
* `displayLocationInDecimal` must be a boolean.
* `scale` must be a non-negative integer.
* `description` and `inlineHelpText` must be strings.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* `scale` must be between 0 and 18 (Salesforce Location field precision limit).
* If `displayLocationInDecimal` is true, the field stores decimal values; otherwise, uses Degrees/Minutes/Seconds.
* `required` false allows `null` values.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **80 characters**.
* `scale` must not exceed Salesforce supported precision for geolocation fields.
* Location fields cannot be marked as unique.
* Encrypted geolocation fields are unsupported.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* If a field exists:

  * Prevent changes to `type` (Location → other types is illegal).
  * Prevent changes to `scale` or `displayLocationInDecimal` if existing data is present.
* Validate that the target object is **not managed-package locked**.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag                      | JSON Key                 | Source | Default Value      |
| ---------------------------- | ------------------------ | ------ | ------------------ |
| `<fullName>`                 | fullName                 | LLM    | Derived from Label |
| `<label>`                    | label                    | LLM    | N/A                |
| `<description>`              | description              | LLM    | Empty String       |
| `<inlineHelpText>`           | inlineHelpText           | LLM    | Empty String       |
| `<required>`                 | required                 | LLM    | false              |
| `<displayLocationInDecimal>` | displayLocationInDecimal | LLM    | false              |
| `<scale>`                    | scale                    | LLM    | 0                  |
| `<trackHistory>`             | trackHistory             | UI     | false              |
| `<type>`                     | type                     | System | Location           |
| `<trackTrending>`            | N/A                      | System | false              |

---

# look-up

# Field Documentation: Lookup Field

This document breaks down the Salesforce **Lookup Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them. It also serves as the specification for building Zod validation for Lookup fields.

---

Stored in `LookUp__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the **primary intent fields**. The LLM must define these to establish the relationship between two objects and how that relationship behaves.

```xml
<fullName>Lookup_Field__c</fullName>
<label>Kitchen Sink Reference</label>
<referenceTo>Kitchen_Sink__c</referenceTo>
<relationshipLabel>Kitchen Sinks</relationshipLabel>
<relationshipName>Kitchen_Sinks</relationshipName>
<deleteConstraint>SetNull</deleteConstraint>
<description>This is the description for the lookup field</description>
<inlineHelpText>This is the help text for the lookup field</inlineHelpText>
```

**Intent breakdown**

* `referenceTo` defines the **parent object** being referenced.
* `relationshipName` defines the **API name** used for child-to-parent traversal.
* `relationshipLabel` defines the **UI label** for the related list.
* `deleteConstraint` controls what happens to child records when the parent is deleted. Valid options for Lookup fields are:

  * `SetNull`: Clears the field on child records when the parent is deleted.
  * `Restrict`: Prevents deletion of the parent record if any child records reference it.

**Future Extension:** Salesforce supports `<lookupFilter>` blocks to restrict selectable records. This is not implemented in v1.

---

## 3. Supporting Features (React Flow UI)

These fields are defaulted by the schema but exposed in the UI so users can tune record behavior.

```xml
<required>false</required>
<trackHistory>false</trackHistory>
```

---

## 4. Others (Transpiler Injected)

These fields are automatically injected by the transpiler to satisfy Salesforce Metadata API requirements.

```xml
<type>Lookup</type>
<trackTrending>false</trackTrending>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
 {
    type: "Formula",
    label: "Asset Status Label",
    fullName: "Asset_Status_Label__c",
    returnType: "Text",
    formula: 'IF(NOT(ISBLANK(Asset_ID__c)), "ACTIVE: " & Name, "INACTIVE")',
    blankOption: "BlankAsZero",
    description: "Human-readable status based on assigned ID.",
    helpText: "Shows ACTIVE or INACTIVE followed by the record name."
 }
```

## 5. Validation Rules

This section documents validation rules applied to **Lookup fields** at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the Lookup field schema.

* `type` must be exactly `Lookup`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `referenceTo`, `relationshipName`, and `relationshipLabel` must be strings.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and relationship correctness.

* `relationshipName` must be provided when `referenceTo` is defined.
* `relationshipLabel` must be provided when `relationshipName` exists.
* `deleteConstraint` must be one of `SetNull` or `Restrict`.
* `referenceTo` must point to an object defined in the schema graph.
* `required = true` is only allowed when `deleteConstraint = Restrict`.

#### Object-Level Logical Validation

* `relationshipName` must be unique across all Lookup and Master-Detail fields on the **parent object**.
* `relationshipName` must not match the parent object's API name.
* Multiple lookups to the same object must use distinct `relationshipName` values.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform limits and rules that cannot be fully expressed in Zod.

* `fullName` (API name excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **40 characters**.
* `relationshipName` must not exceed **40 characters** and must be alphanumeric with underscores.
* `relationshipLabel` must not exceed **80 characters**.
* Lookup fields **cannot be encrypted**.
* Lookup fields cannot be marked as **External IDs**.
* Cross-object relationship depth limits must be respected when referenced by formulas or roll-ups.
* Circular relationships between objects are not allowed.

Violations at this level should surface as **linter errors** with precise remediation guidance.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org prior to deployment.

* Verify that no existing field with the same `fullName` exists on the target object.
* If the field already exists:

  * Prevent changes to `referenceTo`.
  * Prevent changes to `relationshipName`.
  * Prevent conversion between Lookup and Master-Detail.
* Validate that the referenced object exists in the target org and is deployable.
* Validate that the target object is not **managed-package locked**.
* Detect namespace collisions in namespaced orgs.
* Detect destructive delete behavior conflicts with existing data (e.g., `Restrict` with existing child records).

Failures at this level should be reported as **pre-deployment conflicts**, not schema validation errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag               | JSON Key          | Source | Default Value |
| --------------------- | ----------------- | ------ | ------------- |
| `<fullName>`          | fullName          | LLM    | Derived       |
| `<label>`             | label             | LLM    | N/A           |
| `<referenceTo>`       | referenceTo       | LLM    | N/A           |
| `<relationshipName>`  | relationshipName  | LLM    | N/A           |
| `<relationshipLabel>` | relationshipLabel | LLM    | N/A           |
| `<deleteConstraint>`  | deleteConstraint  | LLM    | SetNull       |
| `<description>`       | description       | LLM    | Empty String  |
| `<inlineHelpText>`    | inlineHelpText          | LLM    | Empty String  |
| `<required>`          | required          | UI     | false         |
| `<trackHistory>`      | trackHistory      | UI     | false         |
| `<type>`              | type              | System | Lookup        |
| `<trackTrending>`     | N/A               | System | false         |

---

# master_detail

# Field Documentation: MasterDetail

This document breaks down the Salesforce **Master-Detail Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `master_detail__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the Master-Detail field.

```xml
<fullName>master_detail__c</fullName>
<label>master_detail</label>
<description>master detail description</description>
<inlineHelpText>master-detail-help-text</inlineHelpText>
<referenceTo>Account</referenceTo>
<relationshipLabel>Kitchen Sinks</relationshipLabel>
<relationshipName>Kitchen_Sinks</relationshipName>
<relationshipOrder>0</relationshipOrder>
<reparentableMasterDetail>true</reparentableMasterDetail>
<writeRequiresMasterRead>false</writeRequiresMasterRead>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<trackHistory>false</trackHistory>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>MasterDetail</type>
<trackTrending>false</trackTrending>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "MasterDetail",
  "label": "master_detail",
  "fullName": "master_detail__c",
  "description": "master detail description",
  "inlineHelpText": "master-detail-help-text",
  "referenceTo": "Account",
  "relationshipLabel": "Kitchen Sinks",
  "relationshipName": "Kitchen_Sinks",
  "relationshipOrder": 0,
  "reparentableMasterDetail": true,
  "writeRequiresMasterRead": false
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **Master-Detail** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the Master-Detail field schema.

* `type` must be exactly `MasterDetail`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `referenceTo` must be a valid Salesforce object API name.
* `relationshipLabel` and `relationshipName` must be non-empty strings.
* `relationshipOrder` must be a non-negative integer.
* `reparentableMasterDetail` and `writeRequiresMasterRead` must be booleans.
* `description` and `inlineHelpText` must be strings.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* `relationshipName` must be unique per object.
* `relationshipOrder` defines the display order in roll-up summaries; must match UI expectations.
* `reparentableMasterDetail` true allows child records to be reassigned to different parents.
* `writeRequiresMasterRead` true ensures child record write access respects master record read permissions.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **40 characters**.
* `relationshipName` must be unique per object and not exceed Salesforce limits.
* Master-Detail fields cannot exist if the object already has more than **2 master-detail relationships**.
* `referenceTo` must be a standard or custom object supporting master-detail relationships.
* Cannot mark Master-Detail as `unique` or `externalId`.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* Prevent changes to `type` (MasterDetail → other types is illegal).
* Prevent changes to `referenceTo` if child records exist.
* Ensure `relationshipName` and `relationshipLabel` do not conflict with existing relationships.
* Validate that the target object is **not managed-package locked**.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag                      | JSON Key                 | Source | Default Value      |
| ---------------------------- | ------------------------ | ------ | ------------------ |
| `<fullName>`                 | fullName                 | LLM    | Derived from Label |
| `<label>`                    | label                    | LLM    | N/A                |
| `<description>`              | description              | LLM    | Empty String       |
| `<inlineHelpText>`           | inlineHelpText           | LLM    | Empty String       |
| `<referenceTo>`              | referenceTo              | LLM    | None               |
| `<relationshipLabel>`        | relationshipLabel        | LLM    | None               |
| `<relationshipName>`         | relationshipName         | LLM    | None               |
| `<relationshipOrder>`        | relationshipOrder        | LLM    | 0                  |
| `<reparentableMasterDetail>` | reparentableMasterDetail | LLM    | false              |
| `<writeRequiresMasterRead>`  | writeRequiresMasterRead  | LLM    | false              |
| `<trackHistory>`             | trackHistory             | UI     | false              |
| `<type>`                     | type                     | System | MasterDetail       |
| `<trackTrending>`            | N/A                      | System | false              |

---

# number

# Field Documentation: Number

This document breaks down the Salesforce **Number Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `number__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the number field.

```xml
<fullName>number__c</fullName>
<label>number</label>
<description>number description</description>
<inlineHelpText>number help</inlineHelpText>
<precision>14</precision>
<scale>2</scale>
<required>false</required>
<unique>false</unique>
<isAIPredictionField>true</isAIPredictionField>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<externalId>false</externalId>
<trackHistory>false</trackHistory>
<trackTrending>false</trackTrending>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>Number</type>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "Number",
  "label": "number",
  "fullName": "number__c",
  "description": "number description",
  "inlineHelpText": "number help",
  "precision": 14,
  "scale": 2,
  "required": false,
  "unique": false,
  "isAIPredictionField": true
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **Number** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the Number field schema.

* `type` must be exactly `Number`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `precision` and `scale` must be numbers.
* `required` and `unique` must be boolean.
* `isAIPredictionField` must be boolean if present.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* `precision` must be between 1 and 18 (Salesforce max for Number fields).
* `scale` must be between 0 and `precision`.
* `unique` cannot be true if `externalId` is true.
* `isAIPredictionField` can only be true for Number, Currency, or Percent fields.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **80 characters**.
* `precision` cannot exceed 18.
* `scale` cannot exceed 8 for legacy orgs, 18 in modern orgs.
* Number fields cannot be encrypted if `unique` is true.
* Cannot set `isAIPredictionField` on unsupported field types.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* Prevent changes to `type` if the field already exists.
* Prevent changing `precision` or `scale` if data already exists.
* Validate that the target object is not managed-package locked.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag                 | JSON Key            | Source | Default Value      |
| ----------------------- | ------------------- | ------ | ------------------ |
| `<fullName>`            | fullName            | LLM    | Derived from Label |
| `<label>`               | label               | LLM    | N/A                |
| `<description>`         | description         | LLM    | Empty String       |
| `<inlineHelpText>`      | inlineHelpText      | LLM    | Empty String       |
| `<precision>`           | precision           | LLM    | 18                 |
| `<scale>`               | scale               | LLM    | 0                  |
| `<required>`            | required            | LLM    | false              |
| `<unique>`              | unique              | LLM    | false              |
| `<isAIPredictionField>` | isAIPredictionField | LLM    | false              |
| `<externalId>`          | externalId          | UI     | false              |
| `<trackHistory>`        | trackHistory        | UI     | false              |
| `<trackTrending>`       | trackTrending       | UI     | false              |
---

# percent

# Field Documentation: Percent

This document breaks down the Salesforce **Percent Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `percent__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the percent field.

```xml
<fullName>percent__c</fullName>
<label>percent</label>
<description>percent description</description>
<inlineHelpText>percent help</inlineHelpText>
<precision>14</precision>
<scale>2</scale>
<required>false</required>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<trackHistory>false</trackHistory>
<trackTrending>false</trackTrending>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>Percent</type>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "Percent",
  "label": "percent",
  "fullName": "percent__c",
  "description": "percent description",
  "inlineHelpText": "percent help",
  "precision": 14,
  "scale": 2,
  "required": false
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **Percent** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the Percent field schema.

* `type` must be exactly `Percent`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `precision` and `scale` must be numbers.
* `required` must be boolean.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* `precision` must be between 1 and 18.
* `scale` must be between 0 and `precision`.
* Percent fields logically represent 0–100%, but Salesforce allows storage beyond 100%, so refinement rules can enforce business logic if needed.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **80 characters**.
* `precision` cannot exceed 18.
* `scale` cannot exceed 18.
* Percent fields cannot be encrypted if `unique` is used (not applicable here).

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* Prevent changes to `type` if the field already exists.
* Prevent changing `precision` or `scale` if data already exists.
* Validate that the target object is not managed-package locked.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value      |
| ------------------ | -------------- | ------ | ------------------ |
| `<fullName>`       | fullName       | LLM    | Derived from Label |
| `<label>`          | label          | LLM    | N/A                |
| `<description>`    | description    | LLM    | Empty String       |
| `<inlineHelpText>` | inlineHelpText | LLM    | Empty String       |
| `<precision>`      | precision      | LLM    | 18                 |
| `<scale>`          | scale          | LLM    | 0                  |
| `<required>`       | required       | LLM    | false              |
| `<trackHistory>`   | trackHistory   | UI     | false              |
| `<trackTrending>`  | trackTrending  | UI     | false              |
| `<type>`           | type           | System | Percent            |

---

# phone

# Field Documentation: Phone

This document breaks down the Salesforce **Phone Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `phone__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the phone field.

```xml
<fullName>phone__c</fullName>
<label>phone</label>
<description>phone description</description>
<inlineHelpText>phone help</inlineHelpText>
<required>false</required>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<trackHistory>false</trackHistory>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>Phone</type>
<trackTrending>false</trackTrending>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "Phone",
  "label": "phone",
  "fullName": "phone__c",
  "description": "phone description",
  "inlineHelpText": "phone help",
  "required": false
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **Phone** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the Phone field schema.

* `type` must be exactly `Phone`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `required` must be a boolean.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* Phone fields may be optional or required (`required` = true/false).
* `inlineHelpText` should be consistent with the expected phone format.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **40 characters**.
* Phone fields accept only valid characters allowed by Salesforce (`0-9`, `+`, `-`, `()`, spaces).
* Phone fields **cannot** be encrypted.
* Phone fields **cannot** be used as external IDs.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* If a field exists:

  * Prevent changes to `type` (Phone → other types is illegal).
* Validate that the target object is **not managed-package locked**.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value      |
| ------------------ | -------------- | ------ | ------------------ |
| `<fullName>`       | fullName       | LLM    | Derived from Label |
| `<label>`          | label          | LLM    | N/A                |
| `<description>`    | description    | LLM    | Empty String       |
| `<inlineHelpText>` | inlineHelpText | LLM    | Empty String       |
| `<required>`       | required       | LLM    | false              |
| `<trackHistory>`   | trackHistory   | UI     | false              |
| `<type>`           | type           | System | Phone              |
| `<trackTrending>`  | N/A            | System | false              |

---

# picklist

# Field Documentation: Picklist

This document breaks down the Salesforce **Picklist Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `picklist__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the picklist.

```xml
<fullName>picklist__c</fullName>
<label>picklist</label>
<description>picklist description</description>
<inlineHelpText>picklist help text</inlineHelpText>
<required>false</required>
<valueSet>
    <restricted>true</restricted>
    <valueSetDefinition>
        <sorted>false</sorted>
        <value>
            <fullName>value 1</fullName>
            <default>false</default>
            <label>value 1</label>
        </value>
        <value>
            <fullName>value 2</fullName>
            <default>false</default>
            <label>value 2</label>
        </value>
        <value>
            <fullName>value 3</fullName>
            <default>false</default>
            <label>value 3</label>
        </value>
    </valueSetDefinition>
</valueSet>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<trackHistory>false</trackHistory>
<trackTrending>false</trackTrending>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>Picklist</type>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "Picklist",
  "label": "picklist",
  "fullName": "picklist__c",
  "description": "picklist description",
  "inlineHelpText": "picklist help text",
  "required": false,
  "valueSet": {
    "restricted": true,
    "values": [
      {"fullName": "value 1", "label": "value 1", "default": false},
      {"fullName": "value 2", "label": "value 2", "default": false},
      {"fullName": "value 3", "label": "value 3", "default": false}
    ]
  }
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **Picklist** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the Picklist field schema.

* `type` must be exactly `Picklist`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `required` must be boolean.
* `valueSet` must contain `restricted` boolean and a `values` array.
* Each value object must have `fullName`, `label`, and `default` boolean.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* `restricted` must be true if picklist should prevent values outside the defined set.
* Only one value can have `default` set to true.
* If `sorted` is true, values must be alphabetically ordered.
* `required` fields must not allow empty selection.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **80 characters**.
* Max number of picklist values per field is **1,000**.
* Each value `fullName` must not exceed **40 characters**, label not exceed **255 characters**.
* Salesforce restricts picklist fields with restricted value sets from being updated via API with undefined values.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* Prevent changing `type` of existing picklist fields.
* Prevent removing values that are in use in existing records.
* Validate that the target object is not managed-package locked.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value       |
| ------------------ | -------------- | ------ | ------------------- |
| `<fullName>`       | fullName       | LLM    | Derived from Label  |
| `<label>`          | label          | LLM    | N/A                 |
| `<description>`    | description    | LLM    | Empty String        |
| `<inlineHelpText>` | inlineHelpText | LLM    | Empty String        |
| `<required>`       | required       | LLM    | false               |
| `<valueSet>`       | valueSet       | LLM    | Restricted Picklist |
| `<trackHistory>`   | trackHistory   | UI     | false               |
| `<trackTrending>`  | trackTrending  | UI     | false               |
| `<type>`           | type           | System | Picklist            |

---

# picklist_multi_select

# Field Documentation: Multiselect Picklist

This document breaks down the Salesforce **Multiselect Picklist Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `picklist_multi_select__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the picklist field.

```xml
<fullName>picklist_multi_select__c</fullName>
<label>picklist_multi_select</label>
<description>picklist description</description>
<inlineHelpText>picklist help text</inlineHelpText>
<required>false</required>
<valueSet>
    <restricted>true</restricted>
    <valueSetDefinition>
        <sorted>false</sorted>
        <value>
            <fullName>value 1</fullName>
            <default>false</default>
            <label>value 1</label>
        </value>
        <value>
            <fullName>value 2</fullName>
            <default>false</default>
            <label>value 2</label>
        </value>
        <value>
            <fullName>value 3</fullName>
            <default>false</default>
            <label>value 3</label>
        </value>
    </valueSetDefinition>
</valueSet>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<trackHistory>false</trackHistory>
<visibleLines>4</visibleLines>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>MultiselectPicklist</type>
<trackTrending>false</trackTrending>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "MultiselectPicklist",
  "label": "picklist_multi_select",
  "fullName": "picklist_multi_select__c",
  "description": "picklist description",
  "inlineHelpText": "picklist help text",
  "required": false,
  "valueSet": {
    "restricted": true,
    "sorted": false,
    "values": [
      {"fullName": "value 1", "label": "value 1", "default": false},
      {"fullName": "value 2", "label": "value 2", "default": false},
      {"fullName": "value 3", "label": "value 3", "default": false}
    ]
  }
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **Multiselect Picklist** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the Multiselect Picklist field schema.

* `type` must be exactly `MultiselectPicklist`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `required` must be a boolean.
* `valueSet.values` must be an array of objects containing `fullName`, `label`, and `default`.
* `valueSet.restricted` and `valueSet.sorted` must be booleans.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* `valueSet.restricted` set to `true` enforces that only defined picklist values can be selected.
* `valueSet.sorted` determines whether the picklist options are displayed in sorted order.
* Each `value.default` can only be `true` for at most one value.
* `required` controls whether at least one selection is needed.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **40 characters**.
* Maximum of **1,000 picklist values**.
* Multiselect picklist cannot be unique or an external ID.
* Value labels must be unique and cannot exceed **40 characters**.
* No more than **255 characters** per individual picklist value.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* Prevent changes to `type` (MultiselectPicklist → other types is illegal).
* Prevent changes to picklist values if records already exist using those values.
* Validate that the target object is **not managed-package locked**.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value       |
| ------------------ | -------------- | ------ | ------------------- |
| `<fullName>`       | fullName       | LLM    | Derived from Label  |
| `<label>`          | label          | LLM    | N/A                 |
| `<description>`    | description    | LLM    | Empty String        |
| `<inlineHelpText>` | inlineHelpText | LLM    | Empty String        |
| `<required>`       | required       | LLM    | false               |
| `<valueSet>`       | valueSet       | LLM    | N/A                 |
| `<trackHistory>`   | trackHistory   | UI     | false               |
| `<visibleLines>`   | visibleLines   | UI     | 4                   |
| `<type>`           | type           | System | MultiselectPicklist |
| `<trackTrending>`  | N/A            | System | false               |

---

# text

# Field Documentation: Text

This document breaks down the Salesforce **Text Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `text__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the text field.

```xml
<fullName>text__c</fullName>
<label>text</label>
<description>text description</description>
<inlineHelpText>text help text</inlineHelpText>
<length>20</length>
<required>false</required>
<unique>false</unique>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<externalId>false</externalId>
<trackHistory>false</trackHistory>
<trackTrending>false</trackTrending>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>Text</type>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "Text",
  "label": "text",
  "fullName": "text__c",
  "description": "text description",
  "inlineHelpText": "text help text",
  "length": 20,
  "required": false,
  "unique": false
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **Text** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the Text field schema.

* `type` must be exactly `Text`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `length` must be a number.
* `required` and `unique` must be boolean.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* `length` must be between 1 and 255.
* `unique` cannot be true if `externalId` is true.
* `required` fields must not allow empty values.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **80 characters**.
* `length` cannot exceed 255.
* Text fields with `unique=true` cannot be encrypted.
* `externalId` must be false if `unique` is true.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* Prevent changes to `type` of existing text fields.
* Prevent changing `length` if data already exists.
* Validate that the target object is not managed-package locked.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value      |
| ------------------ | -------------- | ------ | ------------------ |
| `<fullName>`       | fullName       | LLM    | Derived from Label |
| `<label>`          | label          | LLM    | N/A                |
| `<description>`    | description    | LLM    | Empty String       |
| `<inlineHelpText>` | inlineHelpText | LLM    | Empty String       |
| `<length>`         | length         | LLM    | 255                |
| `<required>`       | required       | LLM    | false              |
| `<unique>`         | unique         | LLM    | false              |
| `<externalId>`     | externalId     | UI     | false              |
| `<trackHistory>`   | trackHistory   | UI     | false              |
| `<trackTrending>`  | trackTrending  | UI     | false              |
| `<type>`           | type           | System | Text               |

---

# text_area

# Field Documentation: TextArea

This document breaks down the Salesforce **TextArea Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `text_area__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the text area field.

```xml
<fullName>text_area__c</fullName>
<label>text_area</label>
<description>text area description</description>
<inlineHelpText>text area help</inlineHelpText>
<required>false</required>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<trackHistory>false</trackHistory>
<trackTrending>false</trackTrending>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>TextArea</type>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "TextArea",
  "label": "text_area",
  "fullName": "text_area__c",
  "description": "text area description",
  "inlineHelpText": "text area help",
  "required": false
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **TextArea** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the TextArea field schema.

* `type` must be exactly `TextArea`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `required` must be boolean.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* TextArea fields must not exceed 32,768 characters in length.
* `required` fields must not allow empty values.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **80 characters**.
* TextArea fields cannot be unique.
* Cannot be used as `externalId`.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* Prevent changes to `type` of existing TextArea fields.
* Validate that the target object is not managed-package locked.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value      |
| ------------------ | -------------- | ------ | ------------------ |
| `<fullName>`       | fullName       | LLM    | Derived from Label |
| `<label>`          | label          | LLM    | N/A                |
| `<description>`    | description    | LLM    | Empty String       |
| `<inlineHelpText>` | inlineHelpText | LLM    | Empty String       |
| `<required>`       | required       | LLM    | false              |
| `<trackHistory>`   | trackHistory   | UI     | false              |
| `<trackTrending>`  | trackTrending  | UI     | false              |
| `<type>`           | type           | System | TextArea           |

---

# text_area_encrypted

# Field Documentation: Encrypted Text Area

This document breaks down the Salesforce **Encrypted Text Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `text_area_encrypted__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the encrypted text field.

```xml
<fullName>text_area_encrypted__c</fullName>
<label>text_area_encrypted</label>
<description>text area encrypted description</description>
<inlineHelpText>text area encrypted help</inlineHelpText>
<length>12</length>
<required>false</required>
<maskChar>asterisk</maskChar>
<maskType>ssn</maskType>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<trackHistory>false</trackHistory>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>EncryptedText</type>
<trackTrending>false</trackTrending>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "EncryptedText",
  "label": "text_area_encrypted",
  "fullName": "text_area_encrypted__c",
  "description": "text area encrypted description",
  "inlineHelpText": "text area encrypted help",
  "length": 12,
  "required": false,
  "maskChar": "asterisk",
  "maskType": "ssn"
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **Encrypted Text** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the Encrypted Text field schema.

* `type` must be exactly `EncryptedText`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `length` must be a positive integer.
* `required` must be a boolean.
* `maskChar` must be one of the Salesforce-supported characters (e.g., `asterisk`, `numberSign`).
* `maskType` must be one of the Salesforce-supported types (e.g., `ssn`, `creditCard`, `password`).

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* `length` must comply with Salesforce limits for Encrypted Text fields (1–175 characters).
* `maskType` should correspond to the `maskChar` selected.
* `required` indicates whether a value must be provided.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **40 characters**.
* Maximum `length` for Encrypted Text is **175 characters**.
* Encrypted Text fields **cannot** be unique or external IDs.
* Mask character must be compatible with the selected mask type.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* Prevent changes to `type` (EncryptedText → other types is illegal).
* Validate changes to `length` if existing data exceeds new limits.
* Validate that the target object is **not managed-package locked**.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value      |
| ------------------ | -------------- | ------ | ------------------ |
| `<fullName>`       | fullName       | LLM    | Derived from Label |
| `<label>`          | label          | LLM    | N/A                |
| `<description>`    | description    | LLM    | Empty String       |
| `<inlineHelpText>` | inlineHelpText | LLM    | Empty String       |
| `<length>`         | length         | LLM    | 175                |
| `<required>`       | required       | LLM    | false              |
| `<maskChar>`       | maskChar       | LLM    | asterisk           |
| `<maskType>`       | maskType       | LLM    | ssn                |
| `<trackHistory>`   | trackHistory   | UI     | false              |
| `<type>`           | type           | System | EncryptedText      |
| `<trackTrending>`  | N/A            | System | false              |

---

# text_area_long

# Field Documentation: LongTextArea

This document breaks down the Salesforce **LongTextArea Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `text_area_long__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the LongTextArea field.

```xml
<fullName>text_area_long__c</fullName>
<label>text_area_long</label>
<description>text area long description</description>
<inlineHelpText>text area long help text</inlineHelpText>
<length>32768</length>
<visibleLines>3</visibleLines>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<trackHistory>false</trackHistory>
<trackTrending>false</trackTrending>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>LongTextArea</type>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "LongTextArea",
  "label": "text_area_long",
  "fullName": "text_area_long__c",
  "description": "text area long description",
  "inlineHelpText": "text area long help text",
  "length": 32768,
  "visibleLines": 3
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **LongTextArea** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the LongTextArea field schema.

* `type` must be exactly `LongTextArea`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `length` and `visibleLines` must be numbers.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* `length` must not exceed 32,768 characters.
* `visibleLines` must be a positive integer.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **80 characters**.
* LongTextArea fields cannot be unique.
* Cannot be used as `externalId`.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* Prevent changes to `type` of existing LongTextArea fields.
* Validate that the target object is not managed-package locked.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value      |
| ------------------ | -------------- | ------ | ------------------ |
| `<fullName>`       | fullName       | LLM    | Derived from Label |
| `<label>`          | label          | LLM    | N/A                |
| `<description>`    | description    | LLM    | Empty String       |
| `<inlineHelpText>` | inlineHelpText | LLM    | Empty String       |
| `<length>`         | length         | LLM    | 32768              |
| `<visibleLines>`   | visibleLines   | LLM    | 3                  |
| `<trackHistory>`   | trackHistory   | UI     | false              |
| `<trackTrending>`  | trackTrending  | UI     | false              |
| `<type>`           | type           | System | LongTex            |

---

# text_area_rich

# Field Documentation: Rich Text Area (Html)

This document breaks down the Salesforce **Html (Rich Text Area) Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `text_area_rich__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the rich text area field.

```xml
<fullName>text_area_rich__c</fullName>
<label>text area rich</label>
<description>text area rich description</description>
<inlineHelpText>text area rich help</inlineHelpText>
<length>32768</length>
<visibleLines>25</visibleLines>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<trackHistory>false</trackHistory>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>Html</type>
<trackTrending>false</trackTrending>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "Html",
  "label": "text area rich",
  "fullName": "text_area_rich__c",
  "description": "text area rich description",
  "inlineHelpText": "text area rich help",
  "length": 32768,
  "visibleLines": 25
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **Html (Rich Text Area)** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the Html field schema.

* `type` must be exactly `Html`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `length` must be a positive integer.
* `visibleLines` must be a positive integer.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* `length` must be within Salesforce limits (1–32768 characters for Html fields).
* `visibleLines` controls the default number of visible lines in the UI.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **40 characters**.
* Maximum `length` for Html fields is **32,768 characters**.
* Html fields **cannot** be unique or external IDs.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* Prevent changes to `type` (Html → other types is illegal).
* Validate changes to `length` if existing data exceeds new limits.
* Validate that the target object is **not managed-package locked**.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value      |
| ------------------ | -------------- | ------ | ------------------ |
| `<fullName>`       | fullName       | LLM    | Derived from Label |
| `<label>`          | label          | LLM    | N/A                |
| `<description>`    | description    | LLM    | Empty String       |
| `<inlineHelpText>` | inlineHelpText | LLM    | Empty String       |
| `<length>`         | length         | LLM    | 32768              |
| `<visibleLines>`   | visibleLines   | LLM    | 25                 |
| `<trackHistory>`   | trackHistory   | UI     | false              |
| `<type>`           | type           | System | Html               |
| `<trackTrending>`  | N/A            | System | false              |

---

# time

# Field Documentation: Time

This document breaks down the Salesforce **Time Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `time__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the time field.

```xml
<fullName>time__c</fullName>
<label>time</label>
<description>time description</description>
<inlineHelpText>time help text</inlineHelpText>
<required>false</required>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<trackHistory>false</trackHistory>
<trackTrending>false</trackTrending>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>Time</type>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "Time",
  "label": "time",
  "fullName": "time__c",
  "description": "time description",
  "inlineHelpText": "time help text",
  "required": false
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **Time** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the Time field schema.

* `type` must be exactly `Time`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `required` must be boolean.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* Time fields store values in HH:MM:SS format.
* `required` fields must not allow empty values.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **80 characters**.
* Time fields cannot be unique.
* Cannot be used as `externalId`.

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* Prevent changes to `type` of existing Time fields.
* Validate that the target object is not managed-package locked.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value      |
| ------------------ | -------------- | ------ | ------------------ |
| `<fullName>`       | fullName       | LLM    | Derived from Label |
| `<label>`          | label          | LLM    | N/A                |
| `<description>`    | description    | LLM    | Empty String       |
| `<inlineHelpText>` | inlineHelpText | LLM    | Empty String       |
| `<required>`       | required       | LLM    | false              |
| `<trackHistory>`   | trackHistory   | UI     | false              |
| `<trackTrending>`  | trackTrending  | UI     | false              |
| `<type>`           | type           | System | Time               |

---

# url

# Field Documentation: URL

This document breaks down the Salesforce **URL Custom Field XML** and separates the metadata into categories based on how the transpiler, LLM, and UI will interact with them.

---

Stored in `url__c.field-meta.xml`.

## 1. The Outer Container

This wraps the field definition in Salesforce Source Format (SFDX).

```xml
<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Field content goes here -->
</CustomField>
```

---

## 2. Core Field Configuration (LLM Generated)

These are the primary intent fields. The LLM must decide these to define the business logic and identity of the URL field.

```xml
<fullName>url__c</fullName>
<label>url</label>
<description>url description</description>
<inlineHelpText>url help text</inlineHelpText>
<required>false</required>
```

---

## 3. Supporting Features (React Flow UI)

These fields are not generated by the LLM by default. They are provided as defaults by the Zod schema but are displayed in the React Flow interface in case the user wants to toggle specific platform behaviors.

```xml
<trackHistory>false</trackHistory>
```

---

## 4. Others (Transpiler Injected)

These fields are considered platform-required metadata and are automatically added by the transpiler. They ensure the XML is valid for the Salesforce Metadata API but do not require input from the LLM or the end-user.

```xml
<type>Url</type>
<trackTrending>false</trackTrending>
```

---

## Json Shape

This section represents the core field definition as a JSON object.

```json
{
  "type": "Url",
  "label": "url",
  "fullName": "url__c",
  "description": "url description",
  "inlineHelpText": "url help text",
  "required": false
}
```

---

## 5. Validation Rules

This section documents the validation rules applied to **URL** fields at each validation layer in the transpiler pipeline.

### Level 1: Structural Validation (The Shape)

Ensures the JSON matches the URL field schema.

* `type` must be exactly `Url`.
* `label` must be a non-empty string.
* `fullName` must be a valid Salesforce custom field API name ending in `__c`.
* `required` must be a boolean.

---

### Level 2: Logical Validation (The Context)

Ensures internal consistency and field dependencies.

* `required` indicates whether a value must be provided.
* `inlineHelpText` should provide guidance on expected URL format.

---

### Level 3: Platform Constraints (The Linter)

Enforces hard Salesforce platform rules that are not reliably expressible in Zod schemas and must be checked via custom logic.

* `fullName` (API name, excluding `__c`) must not exceed **40 characters**.
* `label` must not exceed **40 characters**.
* URL fields **cannot** be unique or external IDs.
* URL values must comply with Salesforce-supported URL syntax (e.g., valid scheme `http://` or `https://`).

---

### Level 4: State & Conflict Validation (The Pre-Flight)

An optional runtime validation layer that inspects the target Salesforce org before deployment.

* Verify that no existing field with the same `fullName` already exists on the target object.
* Prevent changes to `type` (Url → other types is illegal).
* Validate that the target object is **not managed-package locked**.
* Detect namespace collisions in namespaced orgs.

Failures at this level should be surfaced as **pre-deployment conflicts**, not schema errors.

---

## 6. Summary Table for Transpiler Logic

| XML Tag            | JSON Key       | Source | Default Value      |
| ------------------ | -------------- | ------ | ------------------ |
| `<fullName>`       | fullName       | LLM    | Derived from Label |
| `<label>`          | label          | LLM    | N/A                |
| `<description>`    | description    | LLM    | Empty String       |
| `<inlineHelpText>` | inlineHelpText | LLM    | Empty String       |
| `<required>`       | required       | LLM    | false              |
| `<trackHistory>`   | trackHistory   | UI     | false              |
| `<type>`           | type           | System | Url                |
| `<trackTrending>`  | N/A            | System | false              |
---

